{% extends 'base.html.twig' %}

{% block title %}Ended Turn{% endblock %}

{% block body %}
    <style>
        *.body {
            text-align: center;
            border: 3px solid green;
        }
    </style>
    <div class="body">
{#        {% if user %}#}
            <h3>Success ending the day/turn!</h3>
            <h4>Continue now below:</h4>
            <a href="{{ path('tribal_management') }}">Back to Game Play Page</a>
{#        {% else %}#}

{#            <h3>#}
{#                You are not logged in. Make an account below:#}
{#            </h3>#}
{#            <a href="{{ path('app_register') }}">Create A New User Account</a>#}
{#            <br>#}
{#        {% endif %}#}
    </div>
{% endblock %}